const module = defineModule()
  .model({
    count: 0,
  })
  .methods(() => ({
    add(value, delay) {
      // setTimeout(() => )
    },
  }))
  .init();

module.subscribe({
  onStateChanged(prev, current) {
    // do something
  },
  onAction(type, params) {},
  // generated by [Actions, Views, State]
  onCountChanged(prev, current) {},
  onAdd(params) {},
});
